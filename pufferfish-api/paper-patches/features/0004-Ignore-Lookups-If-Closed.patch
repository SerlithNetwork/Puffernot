From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Paul <paul@technove.co>
Date: Tue, 6 Jul 2021 17:51:18 -0500
Subject: [PATCH] Ignore Lookups If Closed


diff --git a/src/main/java/org/bukkit/plugin/java/PluginClassLoader.java b/src/main/java/org/bukkit/plugin/java/PluginClassLoader.java
index f5fdeb2090803f9b5509c88b7dfff56cd09edc75..cd650d238a9d3e2e21ca02eeb8cc8460a7ee0774 100644
--- a/src/main/java/org/bukkit/plugin/java/PluginClassLoader.java
+++ b/src/main/java/org/bukkit/plugin/java/PluginClassLoader.java
@@ -51,6 +51,7 @@ public final class PluginClassLoader extends URLClassLoader implements io.paperm
     private io.papermc.paper.plugin.provider.classloader.PluginClassLoaderGroup classLoaderGroup; // Paper
     public io.papermc.paper.plugin.provider.entrypoint.DependencyContext dependencyContext; // Paper
     public JavaPlugin airplane$getPlugin() { return this.plugin; } // Pufferfish - Expose plugin
+    private boolean pufferfish$closed = false; // Pufferfish - Ignore lookups if closed
 
     static {
         ClassLoader.registerAsParallelCapable();
@@ -217,7 +218,7 @@ public final class PluginClassLoader extends URLClassLoader implements io.paperm
         }
         Class<?> result = classes.get(name);
 
-        if (result == null) {
+        if (result == null && !this.pufferfish$closed) { // Pufferfish - Ignore lookups if closed
             String path = name.replace('.', '/').concat(".class");
             // Add details to zip file errors - help debug classloading
             JarEntry entry;
@@ -273,6 +274,7 @@ public final class PluginClassLoader extends URLClassLoader implements io.paperm
             this.setClass(name, result); // Paper
         }
 
+        if (result == null) throw new ClassNotFoundException(name); // Pufferfish - Ignore lookups if closed
         return result;
     }
 
@@ -287,6 +289,7 @@ public final class PluginClassLoader extends URLClassLoader implements io.paperm
             // Paper end
             super.close();
         } finally {
+            this.pufferfish$closed = true; // Pufferfish - Ignore lookups if closed
             jar.close();
         }
     }
